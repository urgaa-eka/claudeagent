# Secrets / local environment — never commit your real key or IDs
.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Node
node_modules/
npm-debug.log*
# scaffold ships lockfile-free; verify.sh regenerates it locally
package-lock.json

# Editor / OS
.DS_Store
